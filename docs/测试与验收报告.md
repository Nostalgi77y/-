# 第一版测试与验收报告

验收日期：2026-07-28

## 自动化结果

| 项目 | 结果 | 说明 |
|---|---|---|
| Java 编译 | 通过 | 46+源文件使用 Java 17 编译 |
| 后端单元测试 | 通过 | 订单状态机、JWT 签发与解析 |
| 管理端 TypeScript 检查 | 通过 | `vue-tsc --noEmit --skipLibCheck` |
| 管理端生产构建 | 通过 | Vite 完成1678个模块转换并生成 `dist` |
| 微信小程序生产构建 | 通过 | uni-app Compiler 5.22 生成 `dist/build/mp-weixin` |
| Docker Compose 语法 | 通过 | 基础与全量 Compose 均通过配置解析 |
| Docker 完整环境 | 通过 | MySQL、Redis、RabbitMQ、MinIO、后端和管理端均正常启动 |
| 端到端业务链路 | 通过 | 登录、购物车、下单、模拟支付、统计全部验证成功 |
| Git 差异格式检查 | 通过 | 未发现空白符错误 |

## 已验收能力

- 管理员登录和角色保护。
- 用户演示登录、分类和在售菜品查询。
- 购物车添加、查询和清空。
- 地址归属校验。
- 下单幂等、服务端价格重算和库存条件扣减。
- 订单明细、用户订单与管理订单查询。
- 模拟支付条件更新。
- RabbitMQ 延迟关单和库存回补代码链路。
- 订单状态机和管理端履约操作。
- WebSocket 订单事件推送。
- 今日营业额、订单、菜品和待处理数据聚合。

## Docker 联调结果

本机 Docker Desktop 已恢复正常。完整环境使用 MySQL `3307`、Redis `6379`、RabbitMQ `5672/15672`、MinIO `9000/9001`、后端 `8080` 和管理端 `80`。已创建并支付一笔28元联调订单，后端健康状态为 `UP`，经营统计正确返回1笔订单与28元营业额。

## 上线前验收清单

- 更换所有默认密钥和密码。
- 使用真实 MySQL/Redis/RabbitMQ 跑一遍下单、支付、接单、配送、完成流程。
- 把延迟队列 TTL 临时改为30秒，验证未支付订单关闭及库存只回补一次。
- 验证重复 `clientOrderNo` 不生成第二个订单。
- 验证普通用户不能访问 `/admin/**`，用户不能读取他人地址和订单。

## 2026-08-13 功能验收

- 管理端新增菜品：通过，保存后用户端菜品接口立即可见。
- 管理端编辑菜品：通过；补充 MyBatis-Plus 乐观锁拦截器后，价格、库存和状态可正常保存。
- 管理端弹窗交互：通过浏览器实际点击验证，新增/编辑弹窗正常打开，编辑数据正确回填，控制台无错误。
- 收货地址：新增、查询、编辑、默认地址及删除接口已完成；小程序页面可直接维护。
- 优惠券：领取、查询、订单门槛校验、使用状态更新已完成。
- 优惠券订单联调：原价 28.00 元，优惠 5.00 元，服务端计算实付 23.00 元，结果正确。
- 重复加购：购物车清空后可再次加入同一菜品，不再触发唯一键冲突。
- 构建验证：后端 Maven 测试、Vue 管理端生产构建、uni-app 微信小程序构建全部通过。

## 2026-08-13 图片与加购修复验收

- 修复逻辑删除购物车记录占用唯一键导致鸡排加购 500 的问题，历史记录可自动恢复。
- 失效用户令牌会在加购时自动重新登录并重试一次。
- 5 个现有菜品均已配置餐品图，公开图片请求全部返回 HTTP 200。
- 管理端表格缩略图、图片选择、上传成功提示和即时预览均通过浏览器实际验证。
- 2.6MB PNG 经 Nginx、Spring Boot 上传成功，持久化图片请求返回 HTTP 200。
- 用户端热销推荐的 3 张图片均加载完成，点击云膳香煎鸡排加购成功且无新增控制台错误。

## 2026-08-13 订单删除验收

- 已取消订单删除后，用户订单数量由 6 条减少为 5 条，目标订单不再出现在用户列表。
- 删除采用用户端隐藏字段，商家管理端仍可查询该订单。
- 重复删除已隐藏订单返回业务错误，不会影响其他订单。
- 待接单订单删除请求被拒绝，返回“进行中的订单不能删除”。
- 页面中 3 条已取消订单显示删除入口，2 条进行中订单不显示删除入口。
- 点击“删除记录”会显示订单号和二次确认弹窗；所有订单均正确显示购买商品明细。
- 配置微信 AppID、HTTPS API 域名和隐私声明后再提交审核。
